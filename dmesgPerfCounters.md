## dmesgPerfCounters

Print values of the runtime's performance counters (a number that keeps track of how many times something happens).
* A counter is simply a number that increases every time an event occurs.
