## BPP in MakeCode Arcade

A **BPP** is short for Bits Per Pixel, which tells how many possible color values are for that one pixel. For example, there are 16 colors in the palette editor, so 4 bits per pixel are required because 2⁴ = 16.
