## pushScene

```sig
   game.pushScene();
````

Pushes a new game scene into the scene stack and switches execution into it.
A scene contains its own data representations, including sprites, backgrounds, event handlers, and more.

## Example

``` ts ```
